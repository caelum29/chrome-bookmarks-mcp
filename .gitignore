# deps & build
node_modules/
dist/
build/
*.mcpb
*.tgz

# runtime data — snapshots live in ~/.cache, but keep any local dumps out
data/
*.sqlite
*.sqlite-journal

# Claude Code harness: shared parts are committed, personal/session state is not
.claude/settings.local.json
.claude/.cc-writes/
.claude/.verify-attempts
.claude/worktrees/

# internal design/research notes kept local (public repo)
docs/dev/

# editor / OS
.DS_Store
.idea/*
!.idea/.gitignore

# logs
*.log
